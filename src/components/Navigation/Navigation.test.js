import { render } from "@testing-library/react";
import Navigation from "./Navigation";

it("should receive 2 links if isHomepage equals true", () => {});
it("should receive link if isHomepage equals false", () => {});
