import Signup from "./Signup";
import { render } from "@testing-library/react";

it("should have label with email address", () => {});
it("should have input validing email address", () => {});
it("should have button for submitting", () => {});
