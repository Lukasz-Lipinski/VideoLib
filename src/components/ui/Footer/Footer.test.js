import Footer from "./Footer";
import { render } from "@testing-library/react";

it("Should have links", () => {});
it("Should have info about author", () => {});
it("Should have address' info", () => {});
